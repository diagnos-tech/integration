# 🇺🇸 Template — never commit real values. Use an external secret operator or
#    `kubectl create secret generic diagnos-api --from-literal=... --from-file=...`.
# 🇧🇷 Modelo — nunca commite valores reais. Use um operador de secrets externo ou
#    `kubectl create secret generic diagnos-api --from-literal=... --from-file=...`.
apiVersion: v1
kind: Secret
metadata:
  name: diagnos-api
type: Opaque
stringData:
  DIAGNOS_API_TOKEN: "apikey-REPLACE_ME"
  OPENBAO_TOKEN: "REPLACE_ME"
  tls.crt: |
    -----BEGIN CERTIFICATE-----
    REPLACE_ME
    -----END CERTIFICATE-----
  tls.key: |
    -----BEGIN PRIVATE KEY-----
    REPLACE_ME
    -----END PRIVATE KEY-----
  clients-ca.pem: |
    -----BEGIN CERTIFICATE-----
    REPLACE_ME
    -----END CERTIFICATE-----
