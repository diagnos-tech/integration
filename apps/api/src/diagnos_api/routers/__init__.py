"""🇺🇸 One `APIRouter` per resource (`patients`, `exams`, `files`, `session`) — `app.py` mounts all four.

🇧🇷 Um `APIRouter` por recurso (`patients`, `exams`, `files`, `session`) — `app.py` monta os quatro.
"""

from __future__ import annotations
