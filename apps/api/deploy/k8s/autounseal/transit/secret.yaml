# 🇺🇸 Template — never commit a real value, same convention as
# `../../secret.yaml`. Create for real with `kubectl create secret generic
# openbao-transit-unseal --from-literal=VAULT_TOKEN=... -n openbao` or an
# external-secrets operator, then skip applying this file.
# 🇧🇷 Modelo — nunca commite um valor real, mesma convenção de
# `../../secret.yaml`. Crie de verdade com `kubectl create secret generic
# openbao-transit-unseal --from-literal=VAULT_TOKEN=... -n openbao` ou um
# operador de secrets externo, e então não aplique este arquivo.
apiVersion: v1
kind: Secret
metadata:
  name: openbao-transit-unseal
type: Opaque
stringData:
  VAULT_TOKEN: "REPLACE_ME"
